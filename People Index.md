# People Index

One note per collaborator: what they own, how they like to work, running threads. A
person note carries `type: person` and `org` frontmatter; the table below renders
from it.

![[People.base#People]]
