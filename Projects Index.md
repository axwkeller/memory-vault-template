# Projects Index

One note per project worth remembering: current initiative, open threads, what got
punted and why, gotchas the repo cannot tell a session. A project note carries
`type: project`, `status` (active | background | done), and `repo` frontmatter; the
table below renders from it, so there is nothing to hand-edit here.

![[Projects.base#Projects]]
